
const BASE_URL = "https://pokeapi.co/api/v2/";

/**
 * Fetch data from the PokeAPI.
 * @param resource The API resource to fetch (e.g., "pokemon").
 * @param identifier The specific resource identifier (e.g., "pikachu").
 */
    